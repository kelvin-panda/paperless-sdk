### 新旧库差异

>meetAnd和meetcoreAnd和PBmeetcoreAnd
>
>这三个库决定了新旧版本

### 记录库更新的原因

#### 20260420
- 文件权限功能修复

#### 20240820

> //20240820
> //fix bug
> 1.解决不能批量保存议题顺序的bug

#### 20240807

更新的库：`libmeetcoreAnd.so`

> //20240807
> //modify
> 1.有客户测试快速签到入会，马上发起同屏没有显示参会人，需要等6秒才行，缩短了获取参会人界面状态的时间

#### 20240805

> //20240805
> //Fix
> 1.解决SDK调用查询成功，释放内存时会出现闪退的bug
>
> //20240731
> //modify
> 1.常用人员支持导入
> 2.常用人员支持批量删除

#### 20240729

> //modify
> 1.增加一种新的会议界面状态交互的逻辑，用于替换现有的交互，现在的不适用于设备较多的情况 需要20240729编译的会议后台，所有终端也要同步更新
> [debug] 
> #启用新的设备会议界面交互
> enablenewfacepost=1



> meetcoresdk
> 1.会场排布支持批量设置 Type_MeetRoomDevPosInfo 使用submit方法即可，只用发送一次，只广播一次通知
> 2.优化代码，之前会场设备移除，修改位置都会触发设备会议查询一次，改为与自己设备ID相等时才查询，调整会场设备布局位置是不需要查询设备会议的
> 1.增加快速导入参会人员的接口， 可以快速添加多个参会人员同时绑定席位，只返回一次通知，同时支持设置导入清空原有参会人员的标志，适用于添加参会人员，导入席位表 详见Type_MemberImport 
> 2.增加批量删除参会人员的接口，删除操作只调用一次指定N个参会人员ID，通知只返回一次，减少冗余的数据通知 详见Type_ModifyMemberPos 使用submit方法即可
> 3.增加批量席位绑定修改的接口，绑定只调用一次指定N个设备绑定，通知只返回一次，减少冗余的数据通知 详见Type_MeetSeatDetailInfo 使用submit方法即可
> 以上新增功能需要使用20240728编译的数据库后台



> //20240726
> //modify
> 1.使用双重断线判断，udp容易丢包，在tcp tick接收的时候强制设置udp断线的间隔为最大值，理论上udp丢包，但是tcp正常，不应该让其掉线
> 2.区域服务器也使用双重断线判断，接收不到udp tick时，如果tcp tick能接收到，也设置udp 断线间隔为最大值

#### 20240727

> //20240727
> //Fix
> 1.解决修改参会人，导致发起同屏时无法显示该参会人的bug
> 2.增加父目录与目录ID相等出现无限递归的bug

#### 20240724

> 20240724
> 1.解决当设备较多时，设备上线过慢，导致设备无法上线，一直超时重链的问题
> 2.增加一些关键的操作日志，用于判断请求的响应定位

#### 2024年7月10日

##### 更新的库

> `libPBmeetcoreAnd.so`,`libmeetcoreAnd.so`,`libCodec.so`,`libExecProc.so`,`libNetClient.so`

##### 更新原因

>//20240709
>//Fix bug
>1、解决自动缓存资料的bug,bug表现上传资料不会自动缓存，需要重启软件才会缓存，结束会议不会停止缓存
>2.增加一个参数，可以自定义设置缓存的速度,在`selfinfo`下的`filecachedelayms`
>3.解决--自动缓存有一个bug，会偶尔出现缓存无效，是SDK出现了死锁，暂时没找到原因，出现后，需要重开软件

#### 2024年6月13日

###### 更新的库

> `libNetClient.so` `libmeetcoreAnd.so` `libPBmeetcoreAnd.so`

###### 更新原因

> 新投票提交问题

#### 2024年5月27日

##### 更新的库

> `libPBmeetcoreAnd.so` `libmeetcoreAnd.so` `libCodec.so` `libExecProc.so`  `libNetClient.so`

##### 更新原因

> 适配方图议题3

#### 2024年4月12日

##### 更新的库

> `libmeetcoreAnd.so`

##### 更新原因

> 解决发送文本广播后，终端收到的`texttype`类型一直是0

#### 2023年10月12日

##### 更新的库

> `libmeetcoreAnd.so`和`libExecProc.so`

##### 更新原因

> 解决以下的问题：
>
> 新建三个**同会议室**的会议： 会议A 14:00 - 20:00、会议B 15:00 - 20:00、会议C 16:00-20:00
>
> 此时选中会议B后进行开始会议，则其它两个会议会进入到已结束会议的状态，并且ABC三个会议的会议预约人都变成空白了；

